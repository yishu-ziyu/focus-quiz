# Focus Quiz 发布前执行计划

更新时间：2026-04-26

## 目标

把 Focus Quiz 从“可以生成题”推进到“生成失败可恢复、作答前有不剧透提示、作答后有证据、错题可迁移到知识库”的状态。

本轮不做社交、账号、云同步、完整知识库和复杂复习算法。原因见 [PRODUCT_STRATEGY_NEXT.md](./PRODUCT_STRATEGY_NEXT.md)：这些方向会扩大竞争面，但不能最快增强 Focus Quiz 的核心差异。

## 决策原则

1. 优先修学习闭环，不扩功能面。
2. 优先 local-first，不引入服务器和账号。
3. 优先可解释题目，不追求更多题量。
4. 优先 Markdown / Obsidian / Anki 这类开放出口，不自建大知识库。

## 本轮范围

### 1. 生成失败恢复

验收标准：

- 大模型返回非法 JSON、空题目或请求失败时，侧边栏出现可操作错误提示。
- 用户能点击“重试生成”直接用同一篇文章重新生成。
- 长文、模型上下文或 Provider 错误时，提示用户可以改用选区模式、缩短文本或换模型。

### 2. Hint / Evidence 分离

验收标准：

- prompt 要求每道题同时返回 `sourceHint` 和 `evidenceQuote`。
- 题目卡片只显示不剧透的 `sourceHint`，标签为 `Hint`。
- 用户可以关闭或重新打开 Hint，偏好保存在本地。
- 用户作答后才显示这道题对应的 `evidenceQuote`，标签为 `Evidence`。
- 错题本保存证据片段和原文链接。

### 3. 模式入口显性化

验收标准：

- 侧边栏提供“选区模式”和“全文模式”两个明确入口。
- 选区模式入口告诉用户选中文本后右键审问。
- 全文模式入口可以直接触发当前网页正文抓取。
- 两个入口在当前模式下有明确视觉状态。

### 4. 错题 Markdown 导出

验收标准：

- 错题本顶部提供“导出 Markdown”。
- 导出内容包含题目、你的答案、正确答案、解释、证据片段、原文标题和原文 URL。
- 为了保持扩展权限克制，本轮先复制到剪贴板，不新增 downloads 权限。

## 暂缓范围

- 单题重生成、降低难度、换成开放题：放到下一轮。
- Anki CSV：等 Markdown 导出体验稳定后再做。
- FSRS 或本地复习调度：分享后再评估。
- 真实浏览器多站点全文抓取测试：功能实现后单独做体验测试清单。

## 验证

- `node --check` 检查核心 JS。
- `python3 -m json.tool manifest.json` 检查 manifest。
- 本地静态预览侧边栏和设置页，检查布局没有明显溢出。
- 手动检查错题 Markdown 字符转义，避免破坏 Markdown 结构。
